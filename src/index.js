import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
// The 'import reportWebVitals from './reportWebVitals';' line is gone.

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// The 'reportWebVitals();' line is gone.